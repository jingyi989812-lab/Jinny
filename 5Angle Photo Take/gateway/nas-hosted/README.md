# Not needed for the Windows Server deployment

These files run the same gateway in Container Manager **on the Synology itself**.
Kept only as a fallback if the Windows Server is ever retired.

For the Windows Server setup, use `install-service.ps1` in the parent folder.
