# Photo Flow — app folder

This folder is what goes on the web server (`index.html` + `api/`). Deployment, API and POS
notes live in the package's `docs/` folder: `FOR-ERIC.md`, `api.md`, `POS-CONNECTION.md`.
